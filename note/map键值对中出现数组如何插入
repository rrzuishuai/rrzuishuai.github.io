//统计字符串下标
法1：
struct elm {
	vector<int> idx;
};
map<char,elm> m;
m[s[i]].idx.push_back(i);

法二:
map<char,vector<int>> m;
m[str[i]].push_back(i);




unordered_multimap<char,int> m;
m.count(c) 本身就能返回某个字符出现的次数
